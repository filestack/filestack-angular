/**
 * Zone.js is required for the default (zone-based) change detection.
 * The library is also zoneless-ready — to try zoneless, remove this import
 * and add `provideZonelessChangeDetection()` in app.config.ts.
 */
import 'zone.js';
